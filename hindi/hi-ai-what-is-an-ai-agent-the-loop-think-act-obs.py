"""AI Agent का जादू ✨
Practice: complete the TODO below, then run the file.
From the Python Shorts channel — subscribe for one concept a day!
"""

while True:
    thought = "Research topic"
    action = "Search Web"
    observation = "Found info"
    print(f"Agent Loop: {thought} -> {action} -> {observation}")
    break
    # TODO: Change the action to 'Call API' and update observation.


# ---------------- SOLUTION (peek only after trying!) ----------------
# while True:
#     thought = "Plan to find coffee"
#     action = "Call barista"
#     observation = "Coffee is ready"
#     print(f"Agent Loop: {thought} -> {action} -> {observation}")
#     break # Stop for demo
