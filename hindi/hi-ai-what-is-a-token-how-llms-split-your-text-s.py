"""LLM Tokens का असली सच! 🧩
Practice: complete the TODO below, then run the file.
From the Python Shorts channel — subscribe for one concept a day!
"""

text = "super_fast"
# TODO: Split the word into smaller parts
parts = text.split("_")
print(parts)


# ---------------- SOLUTION (peek only after trying!) ----------------
# tokens = [t for t in "apple pie".split()]
# # LLM logic: sub-word splitting
# sub_tokens = [t for t in "unbelievable"
#                  .split("e") if t != ""]
# print(sub_tokens)
