"""Python List Transformation 🚀
Practice: complete the TODO below, then run the file.
From the Python Shorts channel — subscribe for one concept a day!
"""

items = ['milk_1', 'bread_2']
# TODO: Use map() and a lambda to remove the underscore from each item.
print(list(map(lambda x: ... , items)))


# ---------------- SOLUTION (peek only after trying!) ----------------
# items = ['apple', 'large_fruit']
# # Pythonic way to remove '_' 
# products = list(map(lambda x: x.replace('_', ''), items))
# print(products)
