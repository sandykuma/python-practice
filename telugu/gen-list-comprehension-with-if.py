"""List Comprehension with 'if' logic 🚀
Practice: complete the TODO below, then run the file.
From the Python Shorts channel — subscribe for one concept a day!
"""

numbers = [1, 2, 3, 4, 5]
# TODO: Create a list of only numbers greater than 3
result = [n for n in numbers if 
    ]


# ---------------- SOLUTION (peek only after trying!) ----------------
# # Pythonic way to filter even numbers
# numbers = [1,  2, 3, 4, 5]
# even_numbers = [n for n in numbers if n % 2 == 0]
# print(even_numbers) # [1, 2, 3, 4, 5] 
# # Wait, the logic is slightly different. Let me re-write the correct code
# # The list comprehension with 'if' allows you to filter elements.
# 
# # Corrected version for the turn of the events
# numbers = [1, 2, 3, 4, 5]
# even_numbers = [n for n in numbers if n % 2 == 0]
# print(even_numbers) # [2, 4]
