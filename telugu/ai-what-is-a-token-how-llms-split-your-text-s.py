"""LLMs టెక్స్ట్‌ని ఎలా చదువుతాయి? 🤖
Practice: complete the TODO, then run it.
From the coding Shorts channel — subscribe for one concept a day!
"""

# Split the word 'reprogramming' into sub-tokens
word = "reprogramming"
# TODO: Use re.findall to split into 're', 'program', 'ming'
tokens =


# ---- SOLUTION (peek only after trying!) ----
# # Simulating subword tokenization
# import re
# text = "unhappiness"
# # Splitting into common sub-units
# tokens = re.findall(r'un|happi|ness', text)
# print(tokens) # ['un', 'happi', 'ness']
